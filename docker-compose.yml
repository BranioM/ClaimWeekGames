services:
  postgres:
    image: postgres:17
    container_name: claimweekgames-postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: claimweekgames
      POSTGRES_PASSWORD: claimweekgames
      POSTGRES_DB: claimweekgames
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

  redis:
    image: redis:8-alpine
    container_name: claimweekgames-redis
    restart: unless-stopped
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data

volumes:
  postgres_data:
  redis_data: